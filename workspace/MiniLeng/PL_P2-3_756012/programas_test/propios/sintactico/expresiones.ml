%-------------------------------------------------------------------
programa expresiones;
%-------------------------------------------------------------------
entero n;

principio

n := a;
n := a > 1;
n := b div 2;
n := a * (b + c) / 100 /20;

n := -n;
n := a or b * (c / d * b);
n := (a or b * (c / d * b)) * (a and b or c * 4 div 2 * (1 + (2 + 3)));

funcion(1, 2);
funcion("1");
funcion;



fin
