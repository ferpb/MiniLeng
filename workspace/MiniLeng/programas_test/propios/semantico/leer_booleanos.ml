programa leer_booleanos;
caracter miCar;

accion leer_bool(ref caracter b);
principio
    escribir("Introduce un booleano: ");
fin

principio
    escribir("Introduce un booleano: ");
    leer(miCar);
    escribir(miCar);
    leer(miCar);
     escribir(miCar);
    leer_bool(miCar);
    escribir(miCar);
fin
